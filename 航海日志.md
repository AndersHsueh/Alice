# 航海日志

# 把一周前的内容进行压缩.近一周的内容保持详细.

## 20260330

`20260330, 09:50` `codex`: 创建航海日志机制，约定后续以“时间、agent、事项”的格式持续记录，供 Alice / Copilot / Claude / Codex 交接上下文。

`20260330, 09:50` `codex`: 完成第一轮收口，更新 [DEVELOPMENT_STRUCTURE.md](/Users/xueyuheng/research/Alice/documents/DEVELOPMENT_STRUCTURE.md)，明确当前现役主链为 `src/index.tsx -> src/ui/** -> src/shim/** -> src/daemon/**`，并补充 `acp-integration`、`nonInteractive` 的状态说明 README。

`20260330, 09:50` `codex`: 给旧 REPL 链补充退役标记，统一运行时版本号读取逻辑，并删除未使用的旧品牌资产 `src/ui/components/AsciiArt.ts`。

`20260330, 09:50` `codex`: 将旧 REPL 链从 `src/cli/**` 与 `src/core/{commandRegistry,builtinCommands,extendedCommands}.ts` 迁移到 `src/legacy-cli/**`，同步修正内部 import，降低现役源码树认知噪音。

`20260330, 09:50` `codex`: 收紧 experimental 边界。现役 `export` 链已不再直接依赖 `acp-integration`；`src/ui/utils/export/collect.ts` 改为自包含实现。`src/utils/nonInteractiveHelpers.ts` 已迁回 `src/nonInteractive/utils/nonInteractiveHelpers.ts`，避免 experimental helper 挂在现役 `src/utils` 下。

`20260330, 09:50` `codex`: 已知状态：本轮做的是结构收口和边界整理，没有执行全量 `npm run build`。原因是仓库中仍存在历史 experimental/类型债；本轮重点不是恢复 experimental 编译，而是让现役主链、legacy、experimental 三者边界清晰。

`20260330, 16:35` `codex`: 新增 [Alice Runtime v2 结构草案](/Users/xueyuheng/research/Alice/documents/设计文档/Alice Runtime v2 结构草案.md)，基于 `deepagentsjs` 对比明确后续方向：保持 daemon-first，在 `src/runtime/**` 新增 runtime kernel，逐步把 agent loop、tool orchestration、workspace backend、task/subagent、scenario pack 从 `src/daemon/**` 中抽出。

`20260330, 17:20` `codex`: 落地 v2-lite 第一刀：新增 `src/runtime/**` 最小骨架（`runtimeTypes`、`runtimeEvents`、`createRuntime`、`agentLoop`、`toolCallState`、`workspace/backend`、`scenarios/scenarioPack`），并把 `src/daemon/chatHandler.ts` 改造成 runtime adapter。核心变化是将对话主循环迁入 `src/runtime/agent/agentLoop.ts`，同时移除 `chatHandler.ts` 中的模块级全局 `toolRecordsBuffer`，改为每次 invocation 独立维护工具状态，降低多 session / 多 channel 并发串扰风险。

`20260330, 17:45` `codex`: 继续收口 v2-lite 的 tool orchestration：新增 `src/runtime/tools/{toolRegistry,toolExecutor,toolResultFormatter}.ts`，把工具注册、执行入口和结果格式化统一收回到 runtime 侧，同时保持底层复用现有 `src/tools/**` 实现。`src/core/llm.ts` 已切到 runtime 工具入口，`src/daemon/services.ts` 改为通过 runtime registry 注册内置工具，`src/shim/hooks/useAliceStream.ts` 也改为复用共享的 `formatToolResult()`，减少前后端各自拼工具展示文案的分散逻辑。

`20260330, 18:05` `codex`: 开始接入真实 `WorkspaceBackend`。新增 `src/runtime/workspace/{localWorkspaceBackend,channelWorkspaceBackend,workspaceResolver}.ts`，并扩展 `ChatStreamRequest.workspaceContext`。当前 runtime 已能根据请求/会话元数据解析本地工作区或通道工作区，`gateway/handler.ts` 会为 channel 消息显式传入 `{ kind: 'channel', channel, chatId }`，`agentLoop` 则会把解析后的 backend 身份与 channel 元数据写回 session metadata，为后续 office workspace / cron workspace 继续接入留出统一入口。

`20260330, 18:20` `codex`: 将 cron workspace 也并入统一 resolver。新增 `src/runtime/workspace/cronWorkspaceBackend.ts`，并在 `workspaceResolver.ts` 提供 `resolveCronWorkspaces()` 与 `readResolvedWorkspaceProfile()`；`src/daemon/taskRunner.ts` 已切换为通过 runtime workspace resolver 获取 cron 扫描目录和 profile，而不再直接依赖 `cronWorkspace.ts` 的扫描读取逻辑。当前 `cronWorkspace.ts` 仍保留 daemon 启动目录与 temp workspace 的基础设施职责，但 cron 的“解析入口”已开始收口到 runtime workspace 层。

`20260330, 18:40` `codex`: 完成两项收尾。其一，新增 `src/runtime/workspace/cronWorkspacePaths.ts`，把 cron 的纯路径解析、temp workspace、profile 读取和相关类型下沉到 runtime workspace 层；`src/daemon/cronWorkspace.ts` 改为兼容 re-export，切断 `runtime -> daemon` 的反向依赖。其二，在 `src/core/session.ts` 加入旧 session 的 workspace metadata 自动回填，并在 `src/runtime/agent/agentLoop.ts` 中补强 metadata 回写条件，避免旧 session 因路径未变化而长期缺少 `workspaceBackendId/workspaceBackendKind`。

`20260330, 18:55` `codex`: 停止继续扩 runtime，转入 review / build 验证。结论：v2-lite 这几轮已完成 `src/runtime/**` 最小骨架、agent loop 抽离、tool orchestration 收口、`local/channel/cron` 三类 workspace resolver 接入、cron 纯逻辑下沉、旧 session workspace metadata backfill。全量 `npm run build` 仍失败，但失败面主要集中在历史类型债区域，如 `src/commands/extensions/**`、`src/commands/mcp/**`、`src/config/**`、`src/ui/hooks/**`、`src/ui/utils/export/**`、`src/services/insight/**`、`src/utils/errors.ts` 等；当前没有证据表明这些 build 失败由本轮 `runtime/chatHandler/taskRunner` 改动直接引入。建议后续接手者（如 Copilot）暂停继续扩 runtime，先按模块分组清理现役主链相关的 build 失败面，重新建立可通过的全量基线。

`20260330, 22:32` `copilot`: 接手后先复核基线，结果与 18:55 记录不同：当前仓库 `npm run build` 可直接通过（`tsc` + `postbuild` 成功）。说明此前“全量 build 失败”状态已不再成立（可能已被后续提交或本地改动修复）。

`20260330, 22:32` `copilot`: 继续做工具链脚本验证，运行 `npm run script:test-tools`。发现测试脚本中的 `searchFiles` 示例使用 `*.ts` 仅匹配当前层目录，导致统计为 0，易误导为工具异常。

`20260330, 22:32` `copilot`: 已修复 `src/scripts/test-tools.ts`：将示例模式由 `*.ts` 改为 `**/*.ts`（递归搜索），并复跑脚本验证通过，当前输出 `searchFiles` 命中 463 个 `.ts` 文件。该修改仅影响示例测试可用性，不改变 runtime/daemon 主链逻辑。

`20260330, 22:34` `copilot`: 继续做 Function Calling 流验证（`npm run script:test-function-calling`）时发现 `searchFiles` 默认 ignore 规则仅写成 `node_modules/**`，会漏掉嵌套目录（如 `prototype/node_modules/**`），导致统计口径偏大。

`20260330, 22:34` `copilot`: 已修复 `src/tools/builtin/searchFiles.ts` 默认忽略项为 `['**/node_modules/**', '**/.git/**', '**/dist/**']`，使任意层级的依赖/构建目录都被排除。随后执行 `npm run build && npm run script:test-tools && npm run script:test-function-calling` 全部通过；Function Calling 统计已从 1116（含嵌套 node_modules）收敛到 668（仅项目文件）。

`20260331, XX:XX` `claude`: 完成 Claude Code 源码学习笔记，输出到 `documents/claude-code-patterns.md`。
覆盖：工具目录结构、token budget 管理、context window 自动压缩（含熔断）、coordinator/worker 分离模式、
StopHooks 机制、bash 命令语义分类、工具可见性白名单。提炼 Alice P0-P3 行动清单共 7 项。
`20260331, 23:59` `claude`: 基于 Claude Code 源码学习，完成异构模型路由设计。输出开发指导到 `documents/heterogeneous-model-router-dev-guide.md`，交接 Copilot 实现。

核心设计：VERONICA 启动时探测所有模型，运行时按任务能力层（format/writing/code/reasoning）动态路由。指数退避熔断（30m→90m，4小时标记作废）。coordinator 也可降级（可用性优先于质量，这是中国大陆场景的务实选择）。TUI 状态栏静默显示当前模型和降级状态。

新增文件：`~/.alice/model_profiles.jsonc`（模型档案，用户可手动注释能力特征）。`alice --test-model` 复用现有探测逻辑，同步更新档案。

配置项：`multi_model_routing`（bool，默认 false），`model_routing`（各能力层首选模型）。

`20260331, 22:52` `copilot`: 补读并更新航海日志。确认今天工作主线不是直接实现功能，而是围绕 Claude Code 源码做 Agent 设计学习、可视化整理，以及反向沉淀为适合 ALICE / VERONICA 的运行时设计草案。

`20260331, 22:52` `copilot`: 完成 Claude Code Agent 架构研究的第一轮产出，新建并加入 `.gitignore` 的目录 `GPT分析ClaudeCode源码/`，生成可直接阅读的 `index.html`，内容包含：Claude Code Agent 架构图、运行流程图，以及与 ALICE / VERONICA 的对照分析。重点提炼：统一 Agent 定义、多来源覆盖、AsyncLocalStorage 上下文隔离、fork/worktree/remote 等特殊模式、memory scope、resume / summary / transcript 生命周期能力。

`20260331, 22:52` `copilot`: 在 `GPT分析ClaudeCode源码/` 下新增 Obsidian 友好的学习笔记 `对ALICE最有价值的10条可落地启发.md`，并配套 `alice-agent-runtime-lessons.svg`。该笔记把 Claude Code 可借鉴的工程思想收敛为 10 条：Agent 定义中心化、默认继承、特殊模式显式化、上下文隔离、可恢复生命周期、工具与权限重组、分层记忆、缓存友好设计、可观测性内建、把 Agent 当任务单元。

`20260331, 22:52` `copilot`: 继续沿“学设计、不抄代码”的原则，新增三篇面向 ALICE 自身演进的讨论稿：`ALICE-AgentProfile设计草案.md`、`VERONICA-任务生命周期设计草案.md`、`ALICE从AI CLI升级到Agent Runtime的路线图.md`。三篇文档共同结论：ALICE 不应复制 Claude Code 的实现细节，而应吸收其 Agent Runtime 方法论，并结合自身 daemon-first、多模型切换韧性、多通道潜力，长出自己的系统形态。

`20260331, 23:51` `copilot`: 根据用户新增的产品理解，修订 `ALICE-AgentProfile设计草案.md`，把初始角色从 5 个扩展到 7 个，正式加入 `consultant` 与 `oracle`。其中 `consultant` 用于帮助用户通过讨论、搜索、论证把模糊需求澄清为可执行任务；`oracle` 用于做结果预演、风险提示与后果分析，强调“预判”而非“神谕式正确答案”。

`20260331, 23:51` `copilot`: 补充仓库级协作纪律，更新 `AGENTS.md`、`.github/copilot-instructions.md`，并新增 `CLAUDE.md`、`QWEN.md`，明确要求各类 agent 进入仓库后先读 `航海日志.md`，避免忽略最近主线和阶段性结论后直接动手“来一刀”。本轮用户要求已将上述学习笔记、设计草案与规则文件纳入版本控制并准备推送远程，便于之后从笔记本继续拉取讨论。
